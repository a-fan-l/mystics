# Mystics 项目 npm 配置

# 默认注册表 (可以通过环境变量或命令行覆盖)
registry=http://localhost:4873

# scoped packages 配置
@mystics:registry=http://localhost:4873

# 其他配置
save-prefix=^
save-exact=false
package-lock=true
shrinkwrap=false

# 发布配置
access=public
tag-version-prefix=v

# 缓存配置
cache-min=86400